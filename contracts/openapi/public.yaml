{
  "openapi": "3.1.0",
  "info": {"title": "Gaokao Public Gateway Baseline", "version": "0.1.0", "description": "Legacy-compatible public endpoints frozen before Go control-plane shadow routing."},
  "paths": {
    "/": {"get": {"summary": "Standalone web entry", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/healthz": {"get": {"summary": "Liveness", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/readyz": {"get": {"summary": "Readiness", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/api/health": {"get": {"summary": "Public health summary", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/api/csrf-token": {"get": {"summary": "Issue browser CSRF token", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/api/gaokao/chat": {"post": {"summary": "Admissions chat", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/api/gaokao/handoff": {"post": {"summary": "Consultation handoff", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}},
    "/api/local/consumer-chat": {"post": {"summary": "Loopback-only local chat", "x-visibility": "internal", "responses": {"default": {"$ref": "#/components/responses/LegacyResponse"}}}}
  },
  "components": {
    "responses": {
      "LegacyResponse": {"description": "Legacy response body is preserved during migration"}
    }
  }
}
