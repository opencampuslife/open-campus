{
  "last_cycle": [
    {
      "task_id": "instrument-bridge",
      "verdict": "accept",
      "reason": "llm_bridge.py 仪器化验证通过：4 计时点，X-Bench header 门控，API key 保护，零开销路径确认。"
    },
    {
      "task_id": "instrument-godot",
      "verdict": "accept",
      "reason": "api_client.gd 仪器化验证通过。发现 const BENCH_MODE=false 问题已热修复为 env var 控制（PERF_BENCH=1）。5 trace 点 + api_latency_capture.gd 已就绪。"
    }
  ],
  "next_cycle": [
    {
      "task_id": "orchestrator",
      "title": "创建 tools/api_latency_bench.py——编排多 mode 采集 + 生成报告",
      "prompt": "基于 /Users/kevinzzz/Documents/database/gaokao-agent/game/metacampus-godot 现有 tools/perf_baseline.py 风格，创建 tools/api_latency_bench.py。\n\n前置注意：BENCH_MODE 现在是 env var 控制，设置 PERF_BENCH=1 来激活（不要改 const）。\n\n编排流程：\n1. 启动 Godot + 等 TestHarness + 启动 LLM Bridge\n2. 对 mock/live/off 各 mode 采集 ≥30 样本（通过 /api/mode?set=... 切换）\n3. live 模式需要设置 PERF_BENCH=1 环境变量（因 api_client.gd 读取）\n4. LLM Bridge bench 模式通过添加 X-Bench: 1 header 激活\n5. 原始数据存 tools/perf_output/perf_api_raw.json\n6. 输出 reports/api-latency-breakdown.md + .json\n7. 报告包含验收表：mode | samples | avg | median | P95 | P99 | error rate | main bottleneck\n8. 报告必须推荐下一步：request queue / response cache / timeout/cancel / bridge optimization / no-op\n\n安全规则：不打印 API key，不提交带 key 代码，live 模式如果不可用跳过即可。",
      "assigned_to": "general",
      "verified_by": "verifier",
      "verify_prompt": "1. api_latency_bench.py 语法正确：python3 -c \"import ast; ast.parse(open('tools/api_latency_bench.py').read())\"\n2. 报告模板包含所有 4 个 mode（mock/live/off/fallback）验收表\n3. 硬编码 secrets？检查无\n4. 输出路径在 reports/ 和 tools/perf_output/ 下",
      "timeout_ms": 600000
    }
  ],
  "plan_complete": false,
  "message_to_user": "Cycle 1 通过。bridge + api_client 仪器化都验收了。BENCH_MODE 已改为环境变量控制，编排器待启动。"
}
