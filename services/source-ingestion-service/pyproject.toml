[project]
name = "metacampus-source-ingestion-service"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "docling",
]
description = "MetaCampus source ingestion service"

[tool.uv]
package = false
