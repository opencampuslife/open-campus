[project]
name = "metacampus-compliance-service"
version = "0.0.0"
requires-python = ">=3.11"
description = "MetaCampus compliance service — regulatory audit, privacy gate"

[tool.uv]
package = false
