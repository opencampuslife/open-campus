# Migration Guardrails

Follow `AGENTS.md` for control-plane work. Public HTTP behavior is frozen in `contracts/routes.yaml`; Phase 0 changes establish contracts and checks only, without moving traffic away from the Python gateway.
